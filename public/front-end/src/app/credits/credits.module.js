(function ()
{
    'use strict';

    angular
        .module('app.credits', [])
        .config(config);
    
        /** @ngInject */
        function config($stateProvider) {
            
        }

})();