(function ()
{
    'use strict';

    angular
        .module('app.configError', [])
        .config(config);
    
        /** @ngInject */
        function config($stateProvider) {
            
        }

})();